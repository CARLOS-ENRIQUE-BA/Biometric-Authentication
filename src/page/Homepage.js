import React from 'react';
import { View, Text } from 'react-native';

const Homepage = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>¡Autenticación completa! Bienvenido a Faunora.</Text>
    </View>
  );
};

export default Homepage;
